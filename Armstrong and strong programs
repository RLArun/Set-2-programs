Program 1:-

num=int(input("enter a number:")) 
count=int(input("Count of the number is:")) 
sum=0
temp=num
for i in range(0, count):
  digit=temp%10
  sum=sum+digit**count
  temp=temp//10
if (sum==num):
   print (sum,"is an Armstrong number") 
else:
   print (sum,"is not an Armstrong number")

Program 2:-

num=int(input('enter a num'))
sum=0
print('the entered number is:')
print(num)
temp = num
while(num):
   i=1
   fact=1
   remainder = num%10
   while(i<=remainder):
      fact=fact*i
      i=i+1
   sum = sum+fact
   num=num//10
if(sum == temp):
   print("The user number is a strong number")
else:
   print("The user number is not a strong number")
